<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hckathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FarmerActivity"
            android:label="Farmer Information"/>
        <activity android:name=".MerchantActivity"
            android:label="Merchant Information"/>
        <activity android:name=".FarmerSellActivity"
            android:label="Sell To Merchant"/>
        <activity android:name=".MerchantSellerActivity"
            android:label="Sell To Farmer"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>